
# 修改下述的字段内容
# 文件名称修改为: secure.yml
# 在 application.yml 中被 import 使用

datasource:
  url: `替换为 jdbc 地址, 如:jdbc:mysql://{db_url}:3306/{db_name}?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai`
  password: `替换为数据库密码`
  username: `替换为数据库账号`
